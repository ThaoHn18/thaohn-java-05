
server:
  port: 8002
spring:
  application:
    name: thaohn-java-app
  config:
    activate:
      on-profile: pro

openapi:
  service:
    api-docs : api-service
    server: http://localhost:${server.port}
    title: Thao HN API Service PRO
    version : 1.0
